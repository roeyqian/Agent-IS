{
  "name": "agentis-worker",
  "private": true,
  "type": "module",
  "license": "MIT",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:migrate:local": "wrangler d1 migrations apply roey-ai-is-base --local",
    "db:migrate:remote": "wrangler d1 migrations apply roey-ai-is-base --remote",
    "typecheck": "tsc --project tsconfig.json"
  },
  "devDependencies": {
    "typescript": "^5.9.2",
    "wrangler": "^4.108.0"
  }
}
